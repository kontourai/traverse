[
  {
    "scope": "author:briananderson1222 branch-prefix:chore/block-hygiene",
    "reason": "Block hygiene from a portfolio audit: creates this repo's first delivery/DECLARED (a repo without one fails bundle-required on its first agent-delivered change, with a message that reads like the change is at fault), adds .github/dependabot.yml watching npm and github-actions, and moves the pinned trust-verify action to the build that names which DECLARED condition failed (flow-agents#1015 — this repo was one of the consumers found still pinned behind it, flow-agents#1018). Config only; no source change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI, including the freshly bumped Trust Verify. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T18:00:00Z"
  },
  {
    "scope": "author:dependabot[bot] branch-prefix:dependabot/",
    "reason": "Dependabot dependency-bump PR; no agent delivery involved, nothing to reconcile. Compound scope binds the platform-set actor identity to the bot's branch namespace per ADR 0022's fork-PR guidance, paired per the 2026-07-03 addendum since branch-prefix alone resolves against a pusher-controlled ref. Added because the dependency watch landed portfolio-wide before any repo except one had this exemption, so the watch's first harvest failed Trust Verify everywhere at once — the gate was right to refuse, and this is the documented route rather than a weakening. Known and accepted cost: if the bot identity rotates this stops matching and dependency PRs break loudly, which the current pinned action reports by naming the failed condition (flow-agents#1015).",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T20:00:00Z"
  },
  {
    "scope": "author:kontourai-releases[bot] branch-prefix:release-please--",
    "reason": "release-please automation PR; no agent delivery involved, nothing to reconcile. Actor verified against live release PRs (opened by the kontourai-releases GitHub App) rather than assumed. Paired with author: per the ADR 0022 2026-07-03 addendum — branch-prefix alone resolves against a pusher-controlled ref. Known and accepted cost: a rotated bot identity stops matching and the release train breaks LOUDLY — the pinned action names the failed condition (flow-agents#1015); thirteen silent days was the bug, a loud break is the correct outcome (flow-agents#1011). Provisioned portfolio-wide after a live release PR failed on exactly this gap — the fourth worked example for flow-agents#1017.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T22:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/release-exemption",
    "reason": "The governance-file change adding the release-automation entry above. Touches delivery/DECLARED only. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T22:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/ts7",
    "reason": "TypeScript 7 with the coupled tsconfig change the bot cannot author: tsgo no longer auto-includes @types/node, so the bump alone fails TS2591 on every node: builtin. Proven on the relay canary (relay#39); full verify green here under the new toolchain. Supersedes the bot's red dev-dependencies PR. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-28T03:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/tv-enforce",
    "reason": "Removes missing-bundle-policy: advisory from the Trust Verify workflow so it inherits the action default (required). Advisory was the adoption stance; with Trust Verify now a required status check in this repo's branch protection, advisory mode is a hole: a live gate probe on a sibling repo (a PR deliberately carrying no bundle and no exemption) passed Trust Verify green and sat MERGEABLE, and this repo runs the identical configuration. One workflow line, no source or behaviour change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the sibling repo's probe re-run under required mode records the negative proof for the shared configuration. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner said fine-to-arm pending working-system evidence; this is the evidence loop)",
    "declared_at": "2026-07-28T19:45:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/flow-agents-local-kit",
    "reason": "Pins @kontourai/flow-agents 5.5.0 as an exact local devDependency and adds the workflow:sidecar / workflow:validate-artifacts scripts, matching the one proven configuration (the marketing-site repo). With bundle-required now enforced portfolio-wide, this repo demanded delivery bundles while providing no declared means to author them: bundle authoring worked only on machines with a coincidental global kit install, and sandboxed delegates without network access could not produce bundles at all. Dev-tooling-only change: no source, no behaviour, no shipped-dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the kit's bins resolve from the local install. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted rollout recommendation)",
    "declared_at": "2026-07-28T21:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:fix/max-chunks-honest-partial",
    "reason": "Producer half of the silent-truncation fix (fieldwork#50): beyond-maxChunks truncation becomes an honest partial stop — partial {reason: max-chunks} set when no earlier stop owns the reason, outcome classifies to {status: partial, reason: max-chunks} through the existing partial path, and classifyWarning gives the truncation warning a distinct {limit, content-truncated} classification instead of sharing benign chunking's {content, content-chunking}. Consumer tolerance shipped first in survey (survey#237) per the exact-pin chain. Agent-delivered outside builder.build in an owner-attended session, so no trust bundle exists. Verification in lieu of a bundle: two new tests pin the truncation-alone partial and the earlier-stop-keeps-its-reason interplay; full suite 367/367 green locally on the built output; this PR's CI runs the same. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted triage recommendations)",
    "declared_at": "2026-07-28T22:50:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/pnpm-migration",
    "reason": "Wave 2 of the org-wide npm -> pnpm 11 move (canary ui#69; plan tracked from station#516). autoInstallPeers is off and here it is load-bearing, not cosmetic: this package declares five OPTIONAL peers and npm installed only three -- @google/genai and openai were never installed. Importing under pnpm's default pulled in 34 packages npm never had (both SDKs plus Google auth, gaxios, jws and protobufjs chains), none of which the suite has been tested against; a fault injection removing the flag and re-resolving re-added @google/genai and changed 72 lockfile entries. With the flag on, the lockfile is npm's set: 145 of 147 identical, zero version drift, zero pnpm-only additions. The two npm entries that do not carry over are @ai-sdk/provider and json-schema, both marked peer=true in package-lock.json (npm auto-installed them as peers of @kontourai/relay and @kontourai/dispatch); neither is imported anywhere in src, tests or scripts, and the full 367-test suite passes without them. Dependency install scripts are blocked by default with esbuild@0.28.1 the one version-pinned allowBuilds entry, added because the first strict install refused with ERR_PNPM_IGNORED_BUILDS naming it; minimumReleaseAge 1440. CI installs with pnpm install --frozen-lockfile after a SHA-pinned pnpm/action-setup in ci.yml, publish-npm.yml and trust-verify.yml (only ci.yml checks out into traverse/, so only it passes package_json_file). Publishing stays on npm publish and npm run verify stays the verify command, so this action's contract string does not move. No shipped source change: the packed file list is identical to the published 0.25.1 tarball (55/55) and no pnpm-* file or lockfile enters it. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green locally (content boundary, typecheck, 367/367 tests, package contents 55 files) and in this PR's own CI; fault injections recorded in the PR body -- a manifest/lockfile mismatch fails with ERR_PNPM_OUTDATED_LOCKFILE, removing the allowBuilds entry fails with ERR_PNPM_IGNORED_BUILDS, and removing autoInstallPeers re-admits the untested SDK graph. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-03 session: owner-directed pnpm migration across all repos, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T01:36:13Z"
  }
]
